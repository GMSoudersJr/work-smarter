import type { PageLoad } from './$types';

export const load: PageLoad = () => {
	return {
		title: 'Randomtimize',
		header: 'OPTOMIZED RANDOMIZER',
		description: 'Randomize your word list as it is updated'
	}
}
